#include "server.h"

void server()
{

}

void createSocket()
{
/*	sockID = socket(PF_INET,SOCK_STREAM,0);
	if(sockID < 0)
		Socket::error();
	else
		std::cout << "Socket created .. " << std::endl;
	*/
}

void destroySocket()
{
	/*
	if(close(sockID) < 0)
		Socket::error();
	else
		std::cout << "Socket closed .. " << std::endl;
	*/
}